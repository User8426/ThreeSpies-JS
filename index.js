/* If there is an error in the code, Bugs, Visual glitches and other stuff, Just select this area and click "Create thread" and address your issue. */

const app = require('express')

const middleware = (req, res, next) => {

} 
app.use(middleware)